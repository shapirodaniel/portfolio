import React from 'react';
import './AboutCard.css';

const AboutCard = ({ card }) => {
	const { img, name, description, href } = card;

	return <div className='aboutCardContainer'>hi im a card</div>;
};

export default AboutCard;
