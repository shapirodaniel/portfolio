import React from 'react';
import './PortfolioCard.css';

const PortfolioCard = ({ card }) => {
	const { img, name, description, href } = card;

	return <div className='portfolioCardContainer'>hi im a card</div>;
};

export default PortfolioCard;
